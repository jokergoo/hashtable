# Intersect a list of vectors to the hash set

Intersect a list of vectors to the hash set

## Usage

``` r
hash_set_intersect_to_list(h, lt, return_counts = FALSE)
```

## Arguments

- lt:

  A list of keys.

- return_counts:

  Whether to only return the number of items?
